# ввод символа от пользователя
symbol = input("введите любой символ: ")

# получаем порядковый номер символа
ascii_code = ord(symbol[0])  # берем первый символ, если введено больше одного

# находим предыдущий и последующий символы
previous_symbol = chr(ascii_code - 1)
next_symbol = chr(ascii_code + 1)

# выводим результаты
print(f"порядковый номер символа '{symbol}' в таблице АСКИ: {ascii_code}")
print(f"предыдущий символ: '{previous_symbol}'")
print(f"последующий символ: '{next_symbol}'")
