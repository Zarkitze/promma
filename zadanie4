import math  # Импорт библиотеки math для математических функций (хотя в этом коде не используется, можно удалить, если не нужно)

def is_in_region(x, y):
    """
    Подсказка: Эта функция проверяет, принадлежит ли точка (x, y) заданной области.
    - Для x >= 0: точка должна быть внутри или на границе круга радиусом 3 с центром в (0, 0).
    - Для x < 0: точка должна быть между линиями y = x + 3 (верхняя) и y = -x - 3 (нижняя).
    Возвращает True, если точка в области; False — иначе.
    """

    # Подсказка: Вычисляем, лежит ли точка внутри круга (x² + y² <= 9). Это используется только для правой части (x >= 0).
    in_circle = x ** 2 + y ** 2 <= 9

    # Подсказка: Если x >= 0 (правая половина плоскости), возвращаем результат проверки круга.
    # Если точка внутри полукруга — True; иначе — False.
    if x >= 0:
        return in_circle

    # Подсказка: Если x < 0 (левая половина), проверяем положение относительно двух прямых линий.
    else:
        # Подсказка: Проверяем, ниже ли точка верхней линии (y <= x + 3). Это верхняя граница области.
        below_upper_line = y <= x + 3
        # Подсказка: Проверяем, выше ли точка нижней линии (y >= -x - 3). Это нижняя граница области.
        above_lower_line = y >= -x - 3
        # Подсказка: Возвращаем True только если точка между обеими линиями (логическое И).
        return below_upper_line and above_lower_line


# Подсказка: Здесь запрашиваем у пользователя координаты точки. float() преобразует ввод в вещественное число.
# Если ввод не число, код упадёт — можно добавить обработку ошибок для улучшения.
x = float(input("Введите координату x: "))
y = float(input("Введите координату y: "))

# Подсказка: Вызываем функцию с введёнными значениями и сохраняем результат (True/False).
result = is_in_region(x, y)

# Подсказка: Выводим результат в консоль. Можно добавить форматирование, например: print("Точка в области:" if result else "Точка вне области")
print(result)
